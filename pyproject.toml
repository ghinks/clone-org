[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "clone-github-org"
version = "1.0.0"
dynamic = ["dependencies"]

[project.scripts]
clone-org = "clone_github_org.main:clone"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}